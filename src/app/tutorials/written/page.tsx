"use client"

import Logo from "@/components/Logo";
import getBlurData from "@/lib/muxblurhash";
import MuxPlayer from "@mux/mux-player-react/lazy";


const page = () => {
  return (
    <main className="pt-[10vh] container">
      <h4 className="flex items-baseline">TUTORIALS</h4>
    </main>
  );
};

export default page;
